# kinda obsolete, don't use this anymore, use this syntax
# <div>: //p[.//img]
# Convert to <div> all <p>s with unsupported elements somewhere inside
@map(//div//p) {
  $p: $@
  @if($p//img) {
    <div>: $p
  }
  @if($p//iframe) {
    <div>: $p
  }
}


